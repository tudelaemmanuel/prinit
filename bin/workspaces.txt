// Create predefined workspaces like that :
// [workspace_name] : [workspace_location]
// The number of spaces must be exactly the same
// Only use '/' in workspace location !

// - Example -
// Desktop : C:/Users/[username]/Desktop/
